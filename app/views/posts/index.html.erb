<div class="news-feed">
  <div class="media">
    <div class="media-body">
      <div class="media-heading">
        <h1><%= t('.welcome') %></h1>
      </div>
      <div class="lead">
        <p><%= t('.lead') %></p>
      </div>
    </div>
  </div>
  <%= render @posts %>
</div>

<% content_for :sidebar do %>
  <%= render 'sidebar' %>
<% end %>
