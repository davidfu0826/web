$('#image-upload-tab').trigger({
  type: 'image_uploaded',
  image: '<%= escape_javascript(@image.image.url) %>',
  image_id: '<%= escape_javascript(@image.id.to_s) %>'
});
$('#image-upload-tab').html("<%= escape_javascript(render('shared/image_form', remote: true)) %>");
$('.select2').select2();
console.log("hej");
$("[type='file']").fileinput({ showUpload: false, showRemove: false });
