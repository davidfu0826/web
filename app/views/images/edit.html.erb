<div class="page-header">
  <h1><%= t(:edit) + " " + t('activerecord.models.image').downcase %></h1>
</div>

<%= render 'form', remote: false %>
<hr>
<%= link_to(t('.all_images'), images_path, class: 'btn btn-default') %>
