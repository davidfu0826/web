<%= bootstrap_form_for @nav_item do |f| %>

  <%= f.collection_select :page_id, @pages, :id, :title, { include_blank: true } %>
  <% if @parent.title == "Top" %>
    <%= f.text_field :title %>
  <% end %>

  <br />
  <%= f.hidden_field :parent_id, value: @parent.id %>
  <%= f.submit %>
<% end %>
