<div class="page-header">
  <h1><%= @page.title %></h1>
</div>

<div class="panel panel-default">
  <div class="panel-body">
    <%= @page.content_html %>
  </div>
</div>

<%= link_to "redigerae", edit_page_path(@page) if can?(:edit, @page) %>
